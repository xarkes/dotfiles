## Bind key
#################
unbind C-b
set -g prefix `      # Not so great on my 60% keyboard :(
# set -g prefix Escape # Too annoying with VIM...
# set -g prefix C-b    # Classic wins I guess
bind-key e send-prefix # Allow BindKey-E to send the actual key
set -sg escape-time 0  # Escape doesn't wait

## Styling
#################
set -g status-position bottom
set -g status-style fg=colour137,bg=colour234,dim
set -g status-left ''
set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
set -g status-right-length 50
set -g status-left-length 20
# set -g default-terminal "xterm-256color"
setw -g window-status-current-style fg=colour81,bg=colour238,bold
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '
setw -g window-status-style fg=colour138,bg=colour235,none
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '
setw -g window-status-bell-style fg=colour255,bg=colour1,bold

## Other
#################
set -g mouse on
bind r source-file ~/.tmux.conf
set-option -g allow-rename off
